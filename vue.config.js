module.exports = {
  publicPath: process.env.NODE_ENV === 'production' ? '/' : '/'
  // publicPath: process.env.NODE_ENV === 'production' ? '/HackFun-client/' : '/'

  // publicPath: process.env.NODE_ENV === 'production' ? '/Hackfun-client/' : process.env.NODE_ENV === 'server' ? './' : '/'
}
